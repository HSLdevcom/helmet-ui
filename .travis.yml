os: windows
language: node_js
node_js:
  - lts/*
install:
  - npm install
script:
  - npm run make
deploy:
  skip_cleanup: true
  provider: script
  script:
    - npm run publish
  on:
    tags: true
    branch: travis-test
